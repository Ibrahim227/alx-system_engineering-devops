#update
sudo apt-get update

# install the ufw
sudo apt-get install -y ufw

# configure ufw to allow port {22, 443 and 80}
sudo ufw allow 22
sudo ufw allow 80
sudo ufw allow 443

# Enable ufw
sudo ufw enable
